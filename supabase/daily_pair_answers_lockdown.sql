-- ⚠️ ЗАПУСКАТЬ ТОЛЬКО ПОСЛЕ ДЕПЛОЯ нового кода и проверки, что Daily Pair
-- Question (ответ вопроса дня, серия, бонус за совпадение) работает через
-- новый эндпоинт /api/pair/daily-answer.
--
-- До этого момента старый фронтенд ещё может писать в daily_pair_answers
-- напрямую — преждевременный revoke сломает приложение.
--
-- SELECT оставляем открытым (экран читает историю/сегодняшние ответы для
-- отображения напрямую) — это не денежная дыра, просто чтение.

revoke insert, update, delete
  on public.daily_pair_answers
  from anon, authenticated;
